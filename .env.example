# Copy this file to `.env` (for local dev) or `.env.production` as needed.

# Option 1: Use absolute API base URLs (client will call these directly):
# For local development
# VITE_API_BASE_URL_LOCAL=http://localhost:3000
# For production
# VITE_API_BASE_URL_PROD=https://api.yourdomain.com

# Option 2: Use Vite dev proxy (keeps client calls relative like /auth/*):
# When using proxy, you can leave the API base URLs unset, and set this instead:
# VITE_PROXY_TARGET=http://localhost:3000

# Notes:
# - If your backend uses a prefix (e.g., /api), set BASE to include it
#   e.g., VITE_API_BASE_URL_LOCAL=http://localhost:3000/api
# - Client endpoints are unprefixed (e.g., /auth/login, /escrow/*, /disputes/*)
#   so the resulting URL becomes: BASE + /auth/login